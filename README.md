# Django + create-react-app boilerplate

```
mkvirtualenv --python=/usr/bin/python3 reactapp
pip install -r requirements.txt
npm install
npm run build
python manage.py migrate
python manage.py runserver
```
